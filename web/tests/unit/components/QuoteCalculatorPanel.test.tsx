import { describe, it } from 'vitest'

describe('QuoteCalculatorPanel', () => {
  it.todo('renders step 1 (calculator) by default')
  it.todo('shows error when monthly bill is empty on submit')
  it.todo('shows error when bill is below minimum')
  it.todo('advances to step 2 (form) with valid bill')
  it.todo('form validates required fields (name, email, phone)')
  it.todo('form validates email format')
  it.todo('advances to step 3 (success) after form submit')
  it.todo('count-up animation plays on step 1 result')
  it.todo('useReducedMotion skips animation')
  it.todo('back button returns from step 2 to step 1')
})
