import { describe, it } from "vitest";

describe("HeaderShell", () => {
  it.todo("renders navigation links from content keys");
  it.todo("shows visible focus styles on interactive elements");
  it.todo("includes home path in primary navigation");
});
