import { test } from '@playwright/test'

test.describe('Home page', () => {
  test.fixme('skip-link is first focusable element', async () => {})
  test.fixme('hero heading is visible', async () => {})
  test.fixme('CTA button navigates to /quote', async () => {})
  test.fixme('services grid renders at least 2 cards', async () => {})
  test.fixme('portfolio cards render with images', async () => {})
  test.fixme('testimonial rail advances on next click', async () => {})
  test.fixme('CTA band is visible below fold', async () => {})
  test.fixme('FAB opens assistant modal', async () => {})
  test.fixme('assistant modal is focusable and closeable', async () => {})
  test.fixme('page is navigable via keyboard only', async () => {})
})
