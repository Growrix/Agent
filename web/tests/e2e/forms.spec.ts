import { test } from '@playwright/test'

test.describe('Quote page', () => {
  test.fixme('step indicator shows 3 steps', async () => {})
  test.fixme('calculator accepts monthly bill input', async () => {})
  test.fixme('invalid bill shows validation error', async () => {})
  test.fixme('valid bill shows savings estimate', async () => {})
  test.fixme('form step validates name field', async () => {})
  test.fixme('form step validates email field', async () => {})
  test.fixme('success step shows after form submit', async () => {})
})

test.describe('Contact page', () => {
  test.fixme('phone channel link has correct href', async () => {})
  test.fixme('WhatsApp channel link has correct href', async () => {})
  test.fixme('email channel link has correct href', async () => {})
  test.fixme('assistant button opens chat modal', async () => {})
  test.fixme('hours table is visible', async () => {})
})

test.describe('Auth pages', () => {
  test.fixme('sign-in page renders email/password form', async () => {})
  test.fixme('sign-up page renders name/email/password form', async () => {})
  test.fixme('sign-up page shows social proof strip', async () => {})
  test.fixme('sign-in link navigates to /sign-up and back', async () => {})
})
