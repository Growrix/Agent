{
  "name": "agent-workspace",
  "private": true,
  "scripts": {
    "dev": "set NEXT_DIST_DIR=.next-dev&& npm --prefix web run dev",
    "build": "set NEXT_DIST_DIR=.next-build&& npm --prefix web run build",
    "start": "set NEXT_DIST_DIR=.next-build&& npm --prefix web run start",
    "lint": "npm --prefix web run lint",
    "typecheck": "npm --prefix web run typecheck",
    "test": "npm --prefix web run test",
    "test:e2e": "npm --prefix web run test:e2e",
    "test:a11y": "npm --prefix web run test:a11y"
  }
}
